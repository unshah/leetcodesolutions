// Given an array nums. We define a running sum of an array as runningSum[i] = sum(nums[0]…nums[i]). Return the running sum of nums.

//Solution:-

var runningSum = function(nums) {
    let runningSum = [];
    let rs = 0;
    for(let i = 0; i< nums.length; i++){
        for(let a = i; a <= i;a++){
            rs += nums[i];
        }
        runningSum[i] = rs;
    }
    return runningSum;
};

// Runtime: 63ms (0.9466)
// Memory: 42.2 MB (0.7322)
